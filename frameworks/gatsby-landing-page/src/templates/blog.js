import * as React from 'react';
import Layout from '../components/Layout/Layout';

const Blog = () => {
  return (
    <Layout>
      this is the blog template
    </Layout>
  );
};

export default Blog;
